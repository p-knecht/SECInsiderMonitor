########## SECInsiderMonitor - Environment Variables ##########
# EXAMPLE FILE -> Change values below and save as .env

### Basic System Information ###
SERVER_FQDN=sim.example.com                 # Fully Qualified Domain Name (FQDN) of the SIM system (required for SSL certificate requests)
SERVER_ACME_MAIL=postmaster@example.com     # Admin email address for ACME/SSL certificate requests

### Initial Web UI Admin Credentials ###
ADMIN_USERNAME=admin                        # Username of the initial admin account
ADMIN_INIT_PASSWORD=[changeme]              # Initial password for the admin account (change after first login)
DISABLE_USER_REGISTRATION=0                 # Set to 1 to disable new user registrations

### Mail Server Settings ###
SMTP_HOST=smtp.example.com                  # Mail server hostname (FQDN)
SMTP_PORT=587                               # Mail server port (typically 587 for STARTTLS or 465 for SSL)
SMTP_USE_SSL=1                              # Use SSL (1 for SSL, 0 for STARTTLS or none)
SMTP_USERNAME=myname@example.com            # Mail server authentication username (leave empty if authentication is not required)
SMTP_PASSWORD=[changeme]                    # Mail server authentication password (leave empty if authentication is not required)
SMTP_FROM_NAME=SECInsiderMonitor            # Display name for outgoing emails
SMTP_FROM_ADDRESS=sim@example.com           # Sender email address for outgoing messages

### Database Root Credentials ###
DB_ROOT_USERNAME=dbroot                     # Root username for database access
DB_ROOT_PASSWORD=[changeme]                 # Root password for database access

### Database Application Settings ###
DB_SIM_USERNAME=sim                         # Username for SIM database access
DB_SIM_PASSWORD=[changeme]                  # Password for SIM database access
DB_SIM_DB=secinsidermonitor                 # Database name used by SIM

### EDGAR Data Access ###
# The User-Agent must be set according to EDGAR's Terms of Service (see chapter 5.2.1)
USER_AGENT=[changeme]                       # Define a User-Agent string (use "" to include spaces in the value)

### Logging / Troubleshooting ###
# Uncomment the following line to enable DEBUG logs
# LOG_LEVEL=debug
