########## SECInsiderMonitor - Environment Variables ##########
# This is an example file. Copy it to `.env` and adjust the values accordingly.

### Basic System Information ###
# Fully Qualified Domain Name (FQDN) of the SIM system
SERVER_FQDN=sim.example.com                 

# Admin email address for ACME/SSL certificate requests
SERVER_ACME_MAIL=postmaster@example.com    

# Set to true to disable new user registrations
SERVER_DISABLE_REGISTRATION=false           


### Mail Server Settings ###
# Mail server hostname (FQDN)
SMTP_HOST=smtp.example.com         

# Mail server port (typically 587 for STARTTLS or 465 for SSL, 25 for unencrypted – not recommended)
SMTP_PORT=587                            

# Use SSL (true for SSL, false for STARTTLS or none)
SMTP_USE_SSL=false

# Mail server authentication username (leave empty if authentication is not required)
SMTP_USERNAME=myname@example.com

# Mail server authentication password (leave empty if authentication is not required)
SMTP_PASSWORD=[changeme]

# Display name for outgoing emails
SMTP_FROM_NAME=SECInsiderMonitor

# Sender email address for outgoing messages
SMTP_FROM_ADDRESS=sim@example.com


### Database Settings ###
# Root username for database access
DB_ROOT_USERNAME=dbroot

# Root password for database access
DB_ROOT_PASSWORD=[changeme]

# Username for SIM database access
DB_SIM_USERNAME=sim

# Password for SIM database access
DB_SIM_PASSWORD=[changeme]

# Database name used by SIM
DB_SIM_DB=secinsidermonitor


### EDGAR Data Access ###
# The User-Agent must be set according to EDGAR's Terms of Service (see chapter 6.2.1)
# Define a custom User-Agent string (enclose in quotes to include spaces)
USER_AGENT=[changeme]


### Logging / Troubleshooting ###
# Uncomment the following line to enable DEBUG logs
# LOG_LEVEL=debug
